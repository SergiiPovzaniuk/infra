route:
  receiver: discord
  group_by: [alertname, instance]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: discord
    webhook_configs:
      - url: http://discord-relay:8080/alerts
