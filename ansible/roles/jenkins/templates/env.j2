JENKINS_ADMIN_ID={{ jenkins_admin_id }}
JENKINS_ADMIN_PASSWORD={{ jenkins_admin_password }}
JENKINS_URL={{ jenkins_url }}/
DOCKERHUB_USERNAME={{ dockerhub_username | default('replace-me') }}
DOCKERHUB_TOKEN={{ dockerhub_token | default('replace-me') }}
GITHUB_PAT={{ github_pat | default('replace-me') }}
GITHUB_USERNAME={{ github_username | default('SergiiPovzaniuk') }}
SES_SMTP_HOST={{ ses_smtp_host }}
SES_SMTP_PORT={{ ses_smtp_port }}
SES_SMTP_USERNAME={{ ses_smtp_username }}
SES_SMTP_PASSWORD={{ ses_smtp_password }}
SES_FROM={{ ses_from }}
SES_TO={{ ses_to }}
